[project]
name = "alert-sms-telegram"
version = "0.1.0"
description = "Cron-driven health checks that alert via Telegram."
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "requests>=2.31",
    "python-telegram-bot>=21.9",
]

[tool.uv]
package = false
